# Test environment
PORT=3001
NODE_ENV=test

# Test database (use a separate test DB)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=giraffe_db
DB_USER=postgres
DB_PASSWORD=dino13

JWT_SECRET=test_secret_key
JWT_EXPIRES_IN=1h

CORS_ORIGIN=http://localhost:5173